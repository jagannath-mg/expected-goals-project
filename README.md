# Expected Goals ML Project

## Frontend (React + Vite)
ML project with Logistic Regression prediction model.

Teammate1 created React frontend using Vite template with:
- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react) for Fast Refresh
- React Compiler enabled
